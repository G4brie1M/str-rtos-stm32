# Startup

This directory contains STM32 startup code.

The startup file defines the initial reset handler, interrupt vector table and low-level initialization required before the C/C++ application starts.

This directory should not be modified unless strictly necessary.
