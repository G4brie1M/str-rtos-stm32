# Core

This directory contains the main STM32CubeIDE application code used in the STR project.

It includes the application entry point, MiROS base files, STM32 interrupt handlers, system initialization code and startup files.

The original structure was preserved from the MiROS STM32 base project to maintain compatibility with STM32CubeIDE.
