# Source

This directory contains source files for the STM32 application.

Main contents:
- `main.cpp`: application entry point
- `miros.cpp`: MiROS implementation
- STM32 initialization and interrupt-related source files

New project modules should be added carefully, preserving the original MiROS/STM32CubeIDE structure.

comando renode: ../renode_1.16.1-dotnet_portable/renode ./str-renode.resc